import { View, Text } from "react-native";

export default function Servers() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Servers Page</Text>
    </View>
  );
}